# Быстрый старт - Premium Kitchens

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Запуск проекта

```bash
npm run dev
```

Откройте браузер по адресу [http://localhost:3000](http://localhost:3000)

## Шаг 3: Структура

- **Главная:** `/` - Полная landing page
- **Каталог:** `/catalog` - Список продуктов
- **Продукт:** `/catalog/[id]` - Детальная информация
- **Галерея:** `/gallery` - Портфолио работ
- **Калькулятор:** `/calculator` - Расчет стоимости

## Часто используемые команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Production
npm start

# Линтинг
npm run lint
```

## Редактирование контента

### Изменить данные продуктов
Откройте `lib/data.ts` и отредактируйте массив `products`

### Изменить цвета
Откройте `tailwind.config.ts` и измените секцию `colors`

### Добавить изображения
1. Поместите файлы в `public/images/`
2. Обновите пути в компонентах
3. Используйте формат WebP для оптимизации

## Развертывание

### Vercel (рекомендуется)
```bash
npm install -g vercel
vercel
```

### Netlify
```bash
npm run build
# Загрузите папку .next на Netlify
```

### Docker
```bash
docker build -t premium-kitchens .
docker run -p 3000:3000 premium-kitchens
```

## Troubleshooting

### Ошибка при установке
```bash
rm -rf node_modules package-lock.json
npm install
```

### Ошибка порта занят
```bash
# Измените порт
PORT=3001 npm run dev
```

### Проблемы с кэшем
```bash
rm -rf .next
npm run dev
```

## Полезные ссылки

- [Next.js Docs](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Lucide Icons](https://lucide.dev/)
- [TypeScript](https://www.typescriptlang.org/docs)

---

Готово! Проект запущен и готов к работе 🚀
